<form method="post" action="{{ path ('cart.add')}}">
        <input type="hidden" name="panier" value="{{ product.id }}">
        <button type="submit" class="btn btn-success float-right mb-2">
            <i class="fas fa-shopping-cart mr-1"></i>
            Ajouter
        </button>
    </form>